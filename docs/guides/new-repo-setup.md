# Setting Up a New Repo for sipag

!!! note "Documentation in progress"
    This guide will be written once the CLI refactor (issues #146–#160) stabilizes.

The short version: add a `CLAUDE.md` to your repo root, create an `approved` label, and ensure your `GH_TOKEN` has write access to the repo.
